<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_github_icon_143196"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_github_icon_143196"
        android:supportsRtl="true"
        android:theme="@style/Theme.App.Starting"
        tools:targetApi="32">
        <activity
            android:name=".ui.setting.SettingActivity"
            android:exported="false" />
        <activity
            android:name=".ui.favorite.FavoriteUserActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.GithubUserApp" />
        <activity
            android:name=".ui.user.UserDetailActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.GithubUserApp" />
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:theme="@style/Base.Theme.GithubUserApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>